{
  "name": "ts-http-status",
  "version": "1.0.0",
  "description": "HTTP status code declarations with type definitions",
  "author": "shogogg <shogo@studiofly.net>",
  "license": "MIT",
  "main": "index.js",
  "typings": "index.d.ts",
  "keywords": [
    "HTTP",
    "Status"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/shogogg/ts-http-status.git"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf *.js *.d.ts",
    "clean:all": "rimraf *.js *.d.ts node_modules",
    "pretest": "npm run build",
    "pretest:watch": "npm run build",
    "test": "mocha --reporter spec --compilers ts:espower-typescript/guess",
    "test:watch": "mocha --reporter spec --compilers ts:espower-typescript/guess -w"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.31",
    "espower-typescript": "^5.0.1",
    "mocha": "^2.5.3",
    "power-assert": "^1.4.1",
    "rimraf": "^2.5.3",
    "typescript": "^1.8.10",
    "typings": "^1.3.1"
  }
}
